[tool.poetry]
name = "argocd-tutorial"
version = "0.1.0"
description = ""
authors = ["ahmed el ghilani"]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.32.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
